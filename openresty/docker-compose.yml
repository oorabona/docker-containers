version: '3'
services:
  openresty:
    build:
      context: .
      args:
        RESTY_IMAGE_TAG: ${VERSION:-latest}
    image: oorabona/openresty:${TAG:-latest}
    ports:
      - 80:80
      - 443:443
    restart: unless-stopped
