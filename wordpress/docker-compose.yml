version: "3"
services:
  wordpress:
    image: oorabona/wordpress:latest
    container_name: wordpress-latest
    build:
      context: .
      args:
        VERSION: latest
        PHP_VERSION: 7.2-fpm
