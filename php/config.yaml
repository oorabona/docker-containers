# PHP container configuration
# Base image and third-party dependency versions

base_image: "${REMOTE_CR}/library/php:${VERSION}"
base_image_cache:
  - source: library/php
    tags_from_versions: true
build_args:
  COMPOSER_VERSION: "2.10.2"
  APCU_VERSION: "5.1.28"
dependency_sources:
  COMPOSER_VERSION:
    lifecycle: tracked
    type: github-release
    repo: composer/composer
    strip_v: false
  APCU_VERSION:
    lifecycle: tracked
    type: github-release
    repo: krakjoe/apcu
    strip_v: true
value_proposition: PHP runtime with common extensions, multi-version retention, ARM support. Pre-built for verifiable provenance.
